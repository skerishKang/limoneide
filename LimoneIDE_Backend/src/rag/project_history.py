async def get_project_history(user_id: str, project_keywords: list, conversations: list) -> list:
    """
    프로젝트 히스토리 반환 (분리됨)
    """
    # 실제 구현은 기존 memory_manager.py의 get_project_history 함수 내용 복사
    return []

def export_conversations(user_id: str, conversations: list) -> str:
    """
    대화 내보내기 (분리됨)
    """
    # 실제 구현은 기존 memory_manager.py의 export_conversations 함수 내용 복사
    return "" 